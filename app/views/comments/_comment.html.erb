<h3><%= comment.comment %></h3>
<p> Published <%= time_ago_in_words(comment.created_at) %> ago by <%= comment.user.email %></p>
<% if user_signed_in? %>
  <%= link_to "Delete Comment", [comment.post, comment], method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-link" %>
<% end %>
